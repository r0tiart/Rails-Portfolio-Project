<h1>User List</h1>

<ul>
	<% @users.each do |user| %>
		<li><%= link_to user.username, user_path(user) %>,
			<%= link_to "User Libray", user_books_path(user.id), class: "userBooks", user_id: user.id %>
		</li>

		<div id="userbooks-<%= user.id %>"></div>

	<% end %>	
</ul>	