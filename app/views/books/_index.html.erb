<% if logged_in?%>
  <ul>
      <li><%= link_to book.title.titleize, book_path(book) %>, by: <%=book.author.name %></li>
  </ul>  
<% else %>
  <ul>
      <li><%= book.title.titleize %>, by: <%=book.author.name %></li>
  </ul>  
<% end %>